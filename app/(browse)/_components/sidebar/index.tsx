import ToggleButton from "./toggleButton";
import Wrapper from "./wrapper";

function SideBar() {
    return (
        <Wrapper>
            sidebar
            <ToggleButton/>
        </Wrapper>
    );
}

export default SideBar;